apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Expo.fullname" . }}-test-connection"
  labels:
{{ include "Expo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "Expo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
