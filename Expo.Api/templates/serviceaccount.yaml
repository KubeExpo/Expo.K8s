# {{- if .Values.serviceAccount.create -}}
# apiVersion: v1
# kind: ServiceAccount
# metadata:
#   name: {{ include "Expo.Api.serviceAccountName" . }}
#   labels:
# {{ include "Expo.Api.labels" . | nindent 4 }}
# {{- end -}}
